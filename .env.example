# Discord Bot Token
DISCORD_TOKEN=your_discord_token

# Discord Client ID (Application ID)
# Required for registering slash commands
DISCORD_CLIENT_ID=your_discord_client_id

# Google AI Studio API Key for Gemini
GEMINI_API_KEY=your_google_ai_studio_key

# Gemini model to use (options: gemini-2.5-pro, gemini-2.5-flash, gemini-3-pro, etc.)
GEMINI_MODEL=gemini-2.5-pro

# Maximum number of tool call iterations before stopping (default: 50)
MAX_ITERATIONS=50

# Maximum execution time for shell commands in milliseconds (default: 60000)
COMMAND_TIMEOUT_MS=60000

# The root directory where the bot is allowed to work/create projects
WORKSPACE_ROOT=/app/workspace

# Log level (optional, defaults to 'info')
LOG_LEVEL=info

# Vercel API Token for deploying web applications (optional)
# Get your token from: https://vercel.com/account/tokens
VERCEL_TOKEN=

# Enable web fetching capability via MCP fetch server (default: true)
# Allows the agent to fetch content from URLs (documentation, APIs, etc.)
ENABLE_WEB_FETCH=true

# Enable Context7 MCP server for up-to-date library documentation (default: true)
# Provides resolve-library-id and get-library-docs tools for latest docs
ENABLE_CONTEXT7=true

# Context7 API Key for higher rate limits (optional)
# Get your key from: https://context7.com/dashboard
CONTEXT7_API_KEY=

# Git user configuration (optional - defaults provided if not set)
# Used for git commits made by the bot
GIT_USER_NAME=Discord Coder Bot
GIT_USER_EMAIL=bot@discord-coder.local
